you have to download model lama from https://cdn-lfs.huggingface.co and put it into models directory
create an conda venv environment
install requirements.txt
run through streamlit run app.py command
